# CLAUDE.md

このファイルは、Claude Code(または他のAIコーディングアシスタント)がこのリポジトリで作業する際の申し送り事項。

## プロジェクト概要

DevOpsキャンプ最終課題として開発している、見積もり振り返り型タスク管理ツールのバックエンドAPI。全体像・進捗は[README.md](./README.md)を参照。

## 開発者について(重要)

**このリポジトリの開発者はアプリケーション開発の初学者。** Go・バックエンド開発・MySQL・シェル操作のいずれも学習中であることを前提に対応すること。

- コマンドを提示するときは、それが何をするコマンドかを一言添える(例: 「DBを作成する」「環境変数を設定する」)
- 専門用語(DSN、ミドルウェア、マイグレーション等)を使うときは、初出時に簡単な補足を入れる
- コマンドはコピペでそのまま動く完全な形で提示する(一部だけ示して「あとは自分で調整して」としない)
- 破壊的な操作(DBのDROP、force pushなど)は必ず事前に確認を取ってから実行する
- コードはAIが書くが、書きっぱなしにしない。実装前後に「なぜこの書き方にするか」を説明し、理解を確認する壁打ちをセットで行う。
- 一方的に完成品を提示せず、設計判断（パッケージ構成・エラーハンドリング・Goのイディオムなど）はその都度合意を取りながら進める。
- 本人から「ここは自分で書きたい」「ここは任せる」といった指示があれば、その都度従う。